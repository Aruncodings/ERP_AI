{
  "name": "erp-ai-query-engine",
  "version": "1.0.0",
  "description": "Natural language AI query engine for Low-Code ERP with MongoDB",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "dev:client": "npm run dev --prefix client",
    "build:client": "npm install --prefix client && npm run build --prefix client",
    "build": "npm run build:client"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "mongodb": "^6.5.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
